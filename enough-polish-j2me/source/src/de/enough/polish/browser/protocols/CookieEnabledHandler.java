//#condition polish.usePolishGui
package de.enough.polish.browser.protocols;

import de.enough.polish.io.CookieManager;

public interface CookieEnabledHandler {
	void setCookieManager(CookieManager cookieManager);
}
